name: Android Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version, for example 1.0"
        required: true
        default: "1.0"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install Android packages
        run: sdkmanager "platforms;android-36" "build-tools;36.1.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"

      - name: Build debug APK
        run: gradle :mobile:assembleDebug --no-daemon --stacktrace

      - name: Prepare release asset
        id: release_meta
        shell: bash
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            raw_version="${GITHUB_REF_NAME}"
          else
            raw_version="${{ inputs.version }}"
          fi

          version="${raw_version#v}"
          tag_name="v${version}"
          apk_name="artplus-mobile-${version}-debug.apk"

          mkdir -p release
          cp mobile/build/outputs/apk/debug/mobile-debug.apk "release/${apk_name}"

          {
            echo "version=${version}"
            echo "tag_name=${tag_name}"
            echo "apk_path=release/${apk_name}"
          } >> "${GITHUB_OUTPUT}"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: artplus-mobile-${{ steps.release_meta.outputs.version }}-debug
          path: ${{ steps.release_meta.outputs.apk_path }}

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ steps.release_meta.outputs.tag_name }}
          VERSION: ${{ steps.release_meta.outputs.version }}
          APK_PATH: ${{ steps.release_meta.outputs.apk_path }}
        shell: bash
        run: |
          {
            echo "ArtPlus Android ${VERSION}"
            echo
            echo "- Built by GitHub Actions."
            echo "- Signed with the default Android debug signing key."
            echo "- APK: $(basename "${APK_PATH}")"
          } > release-notes.md

          if gh release view "${TAG_NAME}" >/dev/null 2>&1; then
            gh release upload "${TAG_NAME}" "${APK_PATH}" --clobber
          else
            gh release create "${TAG_NAME}" "${APK_PATH}" \
              --target "${GITHUB_SHA}" \
              --title "ArtPlus Android ${VERSION}" \
              --notes-file release-notes.md \
              --latest
          fi
