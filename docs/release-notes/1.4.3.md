## What's New

### ✨ Features
- Reworked batch preview page: scrollable layout, framed desktop preview with 4 style tabs, and tuning controls matching the home page.
- "Desktop" background now uses the real system wallpaper (ColorOS default bundled as fallback); custom wallpapers can be uploaded in Settings.
- Smoother secondary-page transitions with dimming and large rounded corners.
- Faster cold start and smoother bottom-bar scrolling via a reworked rendering pipeline.

### 🎨 UI Polish
- General UI polish across the home, preview, settings, presets, and about pages.

### 🔧 Logic Refinements
- Fixed unresponsive buttons and selectors on the About, Settings, Presets, and generation screens; preset lists now refresh instantly after save/edit/delete.
- Logic refinements to preview sampling, caching, and wallpaper loading.

---

## 更新内容

### ✨ 新功能
- 批量预览页重构：滚动布局、带框桌面预览（4 风格切换）、与主页一致的调参区。
- 「桌面」背景使用真实系统壁纸（内置 ColorOS 默认壁纸兜底），设置页可上传自定义壁纸。
- 二级页面转场更顺滑（压暗 + 大圆角推入）。
- 冷启动更快、底栏滑动更顺滑（渲染管线重构）。

### 🎨 UI 优化
- 主页、预览页、设置页、预设页与关于页整体 UI 优化。

### 🔧 逻辑优化
- 修复关于页、设置页、预设页与生成区按钮/选择器无响应；预设存改删后列表即时刷新。
- 预览采样、缓存与壁纸加载等逻辑优化。
