## What's New

### ✨ New Features
- Added bilingual release notes template with separate EN/ZH sections.
- Standardized versions to semver `x.y.z` (e.g., `1.3.0`).
- Included Gradle wrapper for reproducible builds.

### 🐛 Fixes
- Fixed release titles truncated in list (title now `ArtPlus <version>`).
- Fixed floating bottom bar build by switching vendored blur to dependency.

---

## 更新内容

### ✨ 新增功能
- 新增双语发布说明模板，英文在前、中文在后。
- 版本号规范为 `x.y.z` 语义化版本（如 `1.3.0`）。
- 新增 Gradle wrapper，构建更稳定。

### 🐛 修复
- 修复发布列表标题被截断的问题（标题改为 `ArtPlus <version>`）。
- 修复悬浮底栏构建，改用依赖替代本地 vendored 代码。
