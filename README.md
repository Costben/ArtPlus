# ArtPlus Mobile

Android APK project for ArtPlus mobile icon generation and ColorOS ART+ output.

## Build

```bash
gradle :mobile:assembleDebug
```

The GitHub Action in `.github/workflows/android-apk.yml` builds the debug APK and uploads it as an artifact.
